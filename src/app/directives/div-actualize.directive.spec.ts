import { DivActualizeDirective } from './div-actualize.directive';

describe('DivActualizeDirective', () => {
  it('should create an instance', () => {
    const directive = new DivActualizeDirective();
    expect(directive).toBeTruthy();
  });
});
